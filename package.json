{
  "name": "agentdesk",
  "version": "0.1.0",
  "description": "Expose Codex Computer Use as a fleet MCP server",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/possibilities/agentdesk.git"
  },
  "engines": {
    "bun": ">=1.3.14"
  },
  "type": "module",
  "bin": {
    "agentdesk": "src/cli.ts"
  },
  "scripts": {
    "lint": "biome check .",
    "format": "biome check --write src test",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "check": "bun run lint && bun run typecheck && bun run test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.5.2"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3",
    "@types/bun": "1.3.14",
    "bun-types": "1.3.14",
    "typescript": "5.9.3"
  }
}
